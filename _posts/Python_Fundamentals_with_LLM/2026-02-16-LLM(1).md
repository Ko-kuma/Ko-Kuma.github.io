---
layout: post
title: "LLM모델을 활용한 파이썬 학습(1)"
date: 2026-02-16
categories: [Learning_Python_with_LLMs
, Python_Fundamentals_with_LLM]
tags: [Python_Fundamentals_with_LLM]
---
## 초급자용 파이썬 구조 학습 코드(Gemini 이용 코드 생성)

```python
# 1. 클래스 정의 (설계도)
# 객체 지향 프로그래밍의 기초가 되는 부분입니다.
class Beverage:
    def __init__(self, name, price):
        self.name = name
        self.price = price

    def describe(self):
        return f"{self.name} (가격: {self.price}원)"

# 2. 함수 정의 (기능)
# 반복되는 로직을 분리하여 관리합니다.
def display_menu(menu_list):
    print("\n--- 메뉴판 ---")
    for index, item in enumerate(menu_list, start=1):
        # f-string을 이용한 깔끔한 문자열 출력
        print(f"{index}. {item.describe()}")
    print("0. 주문 종료")

# 3. 메인 실행부
# 프로그램이 실제로 시작되는 곳입니다.
def main():
    # 데이터 준비 (리스트와 객체 생성)
    menu = [
        Beverage("아메리카노", 3000),
        Beverage("카페라떼", 3500),
        Beverage("자몽에이드", 4000)
    ]
    
    order_history = []
    total_price = 0

    print("🌟 파이썬 카페에 오신 것을 환영합니다!")

    # 4. 반복문과 조건문 (제어 흐름)
    while True:
        display_menu(menu)
        
        try:
            choice = int(input("\n주문할 번호를 입력하세요: "))
            
            if choice == 0:
                break
            elif 1 <= choice <= len(menu):
                selected_item = menu[choice - 1]
                order_history.append(selected_item.name)
                total_price += selected_item.price
                print(f"✅ {selected_item.name}이(가) 추가되었습니다.")
            else:
                print("❌ 잘못된 번호입니다. 다시 선택해주세요.")
        
        except ValueError:
            print("⚠️ 숫자를 입력해야 합니다!")

    # 5. 결과 출력
    print("\n--- 주문 결과 ---")
    if not order_history:
        print("주문 내역이 없습니다.")
    else:
        print(f"주문 목록: {', '.join(order_history)}")
        print(f"총 결제 금액: {total_price}원")
    
    print("이용해주셔서 감사합니다!")

# 이 코드가 직접 실행될 때만 main()을 호출합니다.
if __name__ == "__main__":
    main()

```

## 코드 실행 순서
* 실행하는 코드의 순서를 알고 싶으면 Thonny라는 프로그램을 활용하여 코드가 어떤 식으로 동작하는지 확인하시면 되겠습니다.
honny와 같은 디버거로 확인했을 때, 파이썬 인터프리터는 다음과 같은 순서로 움직입니다.

1. 설계도와 기능의 로딩 (정의 단계)
컴퓨터는 위에서 아래로 코드를 훑으며 **"나중에 쓸 도구들"**을 메모리에 등록합니다.

class Beverage: "아, 앞으로 음료라는 데이터를 만들 때는 이름과 가격을 세트로 묶겠구나"라고 기억합니다.

def display_menu, def main: "이런 이름의 기능(함수)들이 있군. 나중에 호출하면 실행해야지"라고 준비만 해둡니다. (이 시점엔 아무것도 화면에 출력되지 않습니다.)

2. 진입점 확인: if __name__ == "__main__":
프로그램의 가장 밑바닥에 도달하면 파이썬은 묻습니다. "이 파일이 직접 실행된 메인 파일인가?"

맞다면 main() 함수를 호출하며 본격적인 드라마를 시작합니다. 여기가 실제 프로그램의 **'스타트 버튼'**입니다.

3. 메인 로직의 전개 (main() 내부)
데이터 준비: Beverage("아메리카노", 3000)를 통해 설계도(클래스)에서 실제 객체(인스턴스)를 찍어냅니다.

반복문 시작 (while True): 주문자가 그만둘 때까지 무한히 반복하는 루프에 진입합니다.

함수 호출: display_menu(menu)를 실행하여 화면에 메뉴판을 그립니다.

4. 사용자 상호작용 및 예외 처리
input(): 사용자의 입력을 기다립니다.

try-except: 사용자가 숫자가 아닌 문자를 입력해 '에러(ValueError)'를 발생시켜도, 프로그램이 죽지 않고 다시 기회를 주도록 잡아냅니다.

5. 마무리 및 결과 출력
사용자가 0을 누르면 break를 통해 반복문을 탈출하고, 누적된 order_history와 total_price를 계산하여 영수증을 보여주며 종료됩니다.